# Balagugan S Portfolio

A responsive static portfolio website built from the supplied resume.

## Run locally

Open `index.html` directly, or serve this folder with:

```powershell
python -m http.server 4173
```

Then visit `http://127.0.0.1:4173`.

## Deploy

The folder is ready for any static host such as GitHub Pages, Netlify or Vercel.
